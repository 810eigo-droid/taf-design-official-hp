# TAF Design AI作業ルール

このファイルは、このリポジトリで作業するすべてのAI・自動化エージェント向けの最上位共有事項です。

## ブログの最重要目的

WordPressブログは、単に記事数を増やすためではない。次の成果を目的とする。

1. Googleなどの検索結果で、見込み客の検索意図に合うページとして評価されること
2. ChatGPT、GoogleのAI機能、Bing/CopilotなどのAI検索で、内容を理解・引用されやすくすること
3. TAF Designの専門性と信頼性を示し、相談・見積もりにつなげること

検索順位やAIでの掲載を保証する表現は禁止する。検索エンジン向けの水増しではなく、読者の問題を解決する一次情報・正確性・読みやすさを優先する。

## ブログ作業時の必須成果物

記事HTMLだけを渡して完了にしてはならない。新規作成・大幅更新時は、次を必ずセットで提示・保存する。

- WordPress投稿タイトル
- 推奨スラッグ
- カテゴリー
- WordPressタグ（必要な場合のみ。SEOタイトルをタグ欄へ入れない）
- SEOタイトルタグ
- メタディスクリプション
- robots設定（通常は `index, follow`）
- canonical URL（実際の公開URLと完全一致）
- meta keywordsの扱い（通常は空欄。Googleの順位対策として使わない）
- アイキャッチ画像・OG画像
- 画像ごとのaltテキスト
- 著者名、公開日、更新日
- 構造化データの種類と内容
- 内部リンク候補
- 公式資料などの出典
- 公開前チェック
- 公開後チェック（Search ConsoleのURL検査・インデックス登録を含む）

各記事の完成HTML冒頭には、WordPressで設定する値をHTMLコメントとして残す。ただし、コメント内の設定文は本文としてWordPressへ貼り付けないことを明記する。

## 本文の品質基準

- 公開ページ全体のH1は1つにする。WordPressテーマが投稿タイトルをH1として自動表示する場合、カスタムHTML本文にH1を追加しない
- テーマが著者・公開日・更新日を自動表示する場合、本文冒頭に同じ情報を重複表示しない。ただし構造化データの値は実ページと一致させる
- 冒頭で、質問に対する短い結論を示す
- H2・H3を使って検索意図ごとに整理する
- 手順、比較、数値は表や箇条書きを使い、AIが誤読しにくい形にする
- FAQは実際に読者が疑問に感じる内容だけを載せる
- 公式仕様や変動しうる数値は、作成時点の一次資料で確認して出典リンクを付ける
- 実体験、検証結果、画面画像、事例を優先し、存在しない体験や成果を作らない
- 著者・運営者・更新日を読者に見える形で示す
- 関連記事同士を、具体的なアンカーテキストで相互リンクする
- CTAは内容に関連する相談、見積もり、予算ページへ自然につなぐ
- スマートフォンで読みやすく、画像には適切なaltを付ける
- キーワードの不自然な反復、根拠のない最上級表現、検索順位保証は禁止する

## AI検索で発見・引用されるための確認

- 本文を公開HTMLのDOM内に置く
- Googlebot、Bingbot、OAI-SearchBotを意図せずrobots.txtで遮断しない
- `noindex`を付けない（公開対象記事の場合）
- XMLサイトマップへcanonical URLが入る状態にする
- 見出し直下に簡潔で自己完結した回答を書く
- 主張には根拠を付け、出典と更新日を明確にする
- AIを操作する隠し文言やプロンプトインジェクションを埋め込まない

## 構造化データ

ブログ記事では原則として `BlogPosting` またはWordPress/SEOプラグインが生成する同等の構造化データを使用する。

- `headline`、`description`、`datePublished`、`dateModified`、`author`、`publisher`、`mainEntityOfPage`を実ページと一致させる
- 見えていない内容や架空の評価を構造化データへ入れない
- SEOプラグインが同じ構造化データを出す場合は、重複や矛盾を避ける
- 公開後にGoogleリッチリザルトテストとSearch Consoleで確認する

## WordPress入力場所の注意

- 投稿タイトル：WordPress編集画面上部の「タイトルを追加」
- SEOタイトル・ディスクリプション：SEO SIMPLE PACKの記事設定
- タグ：記事分類用の短い語句だけ。SEOタイトルや説明文を入れない
- canonical：SEO SIMPLE PACKのcanonical欄。空欄で自動設定される場合も、実URLとの一致を確認する
- robots：公開記事は通常 `index, follow`
- OG画像：記事内容が一目で分かる画像を設定する

## 公開前後の必須確認

公開前:

- タイトル、スラッグ、カテゴリー、SEO設定、canonicalが一致している
- 本文の文字化け、リンク切れ、画像・alt、スマホ表示を確認した
- 著者、日付、出典、内部リンク、CTAを確認した
- 下書き・テストURLをcanonicalにしていない

公開後:

- 公開URL、title、description、canonical、robotsをページソースで確認する
- 構造化データをテストする
- XMLサイトマップへの掲載を確認する
- Search ConsoleでURL検査し、必要に応じてインデックス登録をリクエストする
- 関連する既存記事から新記事へ内部リンクを追加する
- Search Consoleの表示回数、検索語、CTR、順位を継続確認し、事実に基づいて改善する

## Git運用

- 記事は `docs/blog-posts/<記事番号>-<slug>/` に保存する
- HTML、原稿、画像、WordPress設定情報を同じ記事番号で管理する
- ユーザーの既存変更を上書きしない
- コミット前に文字化け、HTMLの対応タグ、JSON-LD、`git diff --check`を確認する
- ユーザーからGitHubへの反映まで依頼された場合は、コミットだけで止めず、明示されたブランチへpushし同期状態を報告する

詳細なフォルダ構成と流用手順は `docs/blog-workflow/BLOG-WORKFLOW.md` と `docs/blog-workflow/CONTENT-PROJECT.md` を参照する。
