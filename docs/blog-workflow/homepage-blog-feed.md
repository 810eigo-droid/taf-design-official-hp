# HP下部のブログ自動表示（2026-09-26）

最新6件と全記事一覧を作成。本番反映は未実施。新しいプラグインは不要。
WordPressで公開すると公開日時の新しい順に自動表示。アイキャッチ画像も自動取得。
記事一覧は6件ずつ追加表示。通信失敗時も作成時点の5記事（HPは5記事）を残す。
新しい記事はJavaScript取得で反映されるため、通常のWordPressサイトマップも引き続き利用する。

## 反映手順
1. Xserverのファイルマネージャーで taf-design.com/public_html を開く。
2. 現在の index.html をダウンロードしてバックアップ。
3. 配布ZIPの index.html、css/blog-feed.css、js/blog-feed.js、articles/index.html を同じ位置にアップロード。フォルダ全体を置き換えず、指定4ファイルだけ追加・更新。
4. トップ下部に5件、すべての記事を見るから一覧に5件表示されることを確認。
5. スマホでも確認。元に戻す場合はバックアップの index.html を戻す。

本番用index.htmlは2026-09-26に取得した公開中HTMLをベースにしている。公開HPが後日更新された場合は再生成してから反映する。
Gitのroot index.htmlにはGitHub Pagesのnoindex設定があるため、本番へ直接転送しない。配布版のみ使用。
以後はWPで記事を書き、アイキャッチを設定して公開するだけ。下書きは表示しない。
/articles/ は今回新設。既存投稿URLは変更しない。

公開記事の記録: published-posts.json。記事06（予算）は未公開のまま。

2026-09-26改訂：PCは横3列・最大2行、スマホは横2列。現在は公開5本のため5件表示。カード全体がリンク。表示CSSと取得JSをHTMLにも内蔵し、ファイル単体の確認時にもレイアウトを維持する。今後変更時は外部CSS/JSと各HTML内蔵版を同期する。本番反映は未実施。
